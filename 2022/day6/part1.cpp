#include <iostream>
#include <map>
#include <stack>
#include <stdio.h>
#include <vector>
using namespace std;

// simple stack solution
// read input into stacks, then just follow operations as stacks

int main(void) {
  string line;
  getline(cin, line);

  return 0;
}
